<!DOCTYPE html>
<html>

<head>
  <title>UploadFile kêt quả</title>
</head>

<body>
  <form id="uploadFile" enctype="multipart/form-data">
    <input type="file" name="file">
    <input type="text" name="class" placeholder="Nhập lớp vào đây">
    <button type="submit" formaction="/update">Cập nhật</button>
    <button type="submit" formaction="/replace" formmethod="POST" formenctype="multipart/form-data">Thay thế</button>
    <button type="submit" formaction="/delete" formmethod="POST" formenctype="multipart/form-data">Xóa</button>
  </form>

  <% if(typeof update !== "undefined") {%>
  <% if(update != 0) {%>
  <p>Bạn đã cập nhật <%- update %> người</p>
  <%} %>
  <%}%>
  <% if(typeof create !== "undefined") {%>
  <% if(create != 0 ) {%>
  <p>Bạn đã tạo <%- create %> người</p>
  <%} %>
  <%}%>
  <% if(typeof msg !== "undefined") {%>
  <p><%- msg %></p>
  <%}%>

</body>

</html>